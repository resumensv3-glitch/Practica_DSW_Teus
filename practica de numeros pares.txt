/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {


    val pares = mutableListOf<Int>()
    
    for (i in 1..100)
    {
        if (i % 2 == 0)
        {
           pares.add(i)
        }
    }
    
    println(pares)
}